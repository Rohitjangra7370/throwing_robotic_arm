/.claude
/.venv
.python-version
*.log

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex.gz

# Generated / backup files
paper.zip
graphify-out/

# Python cache
__pycache__/
*.pyc
mc-pilot-pybullet/robot_arm/_urdf_cache/

# Raw IR capture dumps (throw_capture.py / record_and_annotate.py output) --
# large uncompressed frame arrays, regenerable from the camera, not source.
mc-pilot-pybullet/throws/
mc-pilot-pybullet/throws_live/
# per-throw 1 kHz joint position+velocity traces (run_hardware_throw.py throw)
mc-pilot-pybullet/traces/

# Personal / unrelated files that landed in the working tree. This repo is
# PUBLIC -- keep scanned forms, call recordings and stray downloads out of it.
fee_form_filled*.jpeg
instructions/
Screencast from *.webm
post-the-best-cat-memes-*.webp
